; Configuração do npm para o projeto
; Força o uso do npm em vez de outros gerenciadores de pacotes

; Usar package-lock.json sempre
package-lock=true

; Não usar workspaces se não estiver configurado
legacy-peer-deps=false

; Engine strict - respeitar engines no package.json
engine-strict=true
